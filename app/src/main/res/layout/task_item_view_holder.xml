<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/task_type_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintBottom_toBottomOf="@+id/task_name_text_view"
        app:layout_constraintEnd_toStartOf="@+id/task_name_text_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/task_name_text_view"
        tools:src="@drawable/general" />

    <TextView
        android:id="@+id/task_name_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/task_type_image_view"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Get Test" />

    <TextView
        android:id="@+id/task_description_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/task_name_text_view"
        app:layout_constraintTop_toBottomOf="@+id/task_name_text_view"
        tools:text="Get tested for covid. Make sure it is a lateral flow test and the result is received. Call 119 if the result isn't sent via email within 24 hours." />
</androidx.constraintlayout.widget.ConstraintLayout>